import styled from "styled-components";

const Container = styled.div`
  border: 1px solid var(--color-primary-opaque--dark);
  box-shadow: 0 0 10px var(--color-primary--dark);
  width: 100%;
  z-index: 100;
`;

export default Container;
